name: sce

on:
  workflow_dispatch:

jobs:
  call-workflow-passing-data:
    uses: ThirteenAG/WidescreenFixesPack/.github/workflows/all.yml@master
    with:
      tag_list: sce
      project: /t:SplinterCellEssentials_PPSSPP_DualAnalogPatch
