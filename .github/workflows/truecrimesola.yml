name: truecrimesola

on:
  workflow_dispatch:

jobs:
  call-workflow-passing-data:
    uses: ThirteenAG/WidescreenFixesPack/.github/workflows/all.yml@master
    with:
      tag_list: truecrimesola
      project: /t:TrueCrimeStreetsofLA_WidescreenFix
