name: thegodfather

on:
  workflow_dispatch:

jobs:
  call-workflow-passing-data:
    uses: ThirteenAG/WidescreenFixesPack/.github/workflows/all.yml@master
    with:
      tag_list: thegodfather
      project: /t:TheGodfather_WidescreenFix
