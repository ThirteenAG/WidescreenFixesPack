name: truecrimenyc

on:
  workflow_dispatch:

jobs:
  call-workflow-passing-data:
    uses: ThirteenAG/WidescreenFixesPack/.github/workflows/all.yml@master
    with:
      tag_list: truecrimenyc
      project: /t:TrueCrimeNewYorkCity_WidescreenFix
