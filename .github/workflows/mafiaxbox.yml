name: mafiaxbox

on:
  workflow_dispatch:

jobs:
  call-workflow-passing-data:
    uses: ThirteenAG/WidescreenFixesPack/.github/workflows/all.yml@master
    with:
      tag_list: mafiaxbox
      project: /t:Mafia_CXBXR_WidescreenFix
